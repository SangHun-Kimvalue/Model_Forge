"""Model Forge user-facing applications.

Phase 6 introduces ``apps.orchestrator`` — the FastAPI public API that
fronts the agent graph. Modules under ``modules/`` remain reusable
libraries; ``apps/`` hosts the deployable surfaces (HTTP, CLI, etc.).
"""
