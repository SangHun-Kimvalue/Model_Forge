# cad_mechanical prompts

Phase 4 placeholder per DESIGN.md §9 directory tree and §P12 (prompts split
from glue code). The real prompt templates land alongside the first
LLM-driven adapter (e.g. `cadquery_adapter`) so the template format can be
co-designed with the actual DSL constraints and self-healing feedback loop.

Until then, the `cad_coder` agent owns prompt construction for mechanical
CAD code generation; this directory exists to reserve the structural slot.
