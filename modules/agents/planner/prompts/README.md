# Planner Prompts

This directory is reserved for prompt templates used by future
`PromptBasedPlannerAgent` implementations (DESIGN.md §9 tree).

Phase 2 only ships the Mock adapter, which is deterministic and does not need
prompt templates. Add concrete `.md` / `.jinja` templates here when the first
non-mock planner adapter is implemented.
