# Self-Healer Prompts

Reserved for prompt templates used by future `PromptBasedSelfHealerAgent`
implementations (DESIGN.md §9 tree). Per-error_kind revision strategies
(cad_code_error / topology_error / timeout / other) live here when the first
non-mock adapter lands.

Phase 2 only ships the Mock adapter.
